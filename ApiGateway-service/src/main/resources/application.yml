server:
  port: 9090
spring:
  cloud:
    discovery:
      reactive:
        enabled: true
      routes:
        - id: user_management_route # User Management Service
          uri: http://localhost:8085
          predicates:
            - Path=/api/usermanagement/**